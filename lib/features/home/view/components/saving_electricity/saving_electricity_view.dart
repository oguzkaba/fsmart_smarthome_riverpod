import 'package:flutter/material.dart';

class SavingElectricityView extends StatelessWidget {
  const SavingElectricityView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SavingElectricityView')),
    );
  }
}
