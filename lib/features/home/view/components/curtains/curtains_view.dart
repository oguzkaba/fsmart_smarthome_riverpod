import 'package:flutter/material.dart';

class CurtainsView extends StatelessWidget {
  const CurtainsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('CurtainsView')),
    );
  }
}
