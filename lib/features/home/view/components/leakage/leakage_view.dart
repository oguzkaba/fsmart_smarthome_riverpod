import 'package:flutter/material.dart';

class LeakageView extends StatelessWidget {
  const LeakageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('LeakageView')),
    );
  }
}
