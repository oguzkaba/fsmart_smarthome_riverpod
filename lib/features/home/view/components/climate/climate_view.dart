import 'package:flutter/material.dart';

class ClimateView extends StatelessWidget {
  const ClimateView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ClimateView')),
    );
  }
}
