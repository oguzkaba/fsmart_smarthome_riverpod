import 'package:flutter/material.dart';

class SecuritySystemView extends StatelessWidget {
  const SecuritySystemView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SecuritySystemView')),
    );
  }
}
